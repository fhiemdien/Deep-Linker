import React from 'react';

// This component is deprecated as we moved to auto-redirect logic.
// Kept as placeholder to avoid breaking imports if any.

const DeepLinkModal: React.FC<any> = () => {
  return null;
};

export default DeepLinkModal;