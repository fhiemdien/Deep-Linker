# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anonymous-key-here

# Optional: Supabase Service Role Key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Installation Instructions:
# 1. Sign up at https://supabase.com
# 2. Create a new project
# 3. Go to Project Settings > API
# 4. Copy your project URL and anonymous key
# 5. Create a table named 'click_tracking' with these columns:
#    - id (int8, primary key, auto increment)
#    - link_type (text) - 'spotify', 'apple_music', 'youtube'
#    - country (text) - country code from IP
#    - user_agent (text) - browser user agent
#    - platform (text) - which platform the user clicked
#    - timestamp (timestamptz) - when the click happened
#    - deep_link (text) - the actual deep link URL
#    - created_at (timestamptz, auto-generated)
#
# 6. Rename this file to .env.local and fill in your credentials
